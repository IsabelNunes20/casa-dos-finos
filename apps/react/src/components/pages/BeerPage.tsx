// TODO: Create a the Beer Page templates that passes down the details component to Layout's main
// TODO: Create a Detail component that displays one beer detail
import React from "react";
import { Table } from "@radix-ui/themes";


const BeerPage: React.FC = ({ }) => {
  return (
    <Table.Row>
    </Table.Row>
  );
};

export default BeerPage;

